<div class="col-xs-12">
    <h4>登録内容の編集</h4>
    
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="form-group">
          <%= f.label :ニックネーム, class: "col-xs-6" %>
          <%= f.text_field :name, class: "form-control", style: "display: inline-block; width: auto;" %>
        </div>

        <div class="form-group">
          <%= f.label :メールアドレス, class: "col-xs-6" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", style: "display: inline-block; width: auto;" %>
        </div>

        <div class="form-group">
          <%= f.label :パスワード, class: "col-xs-6" %>
          <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control", style: "display: inline-block; width: auto;" %>
        </div>

        <div class="user-edit-message">
          <p>パスワードも変更をご希望の場合は下記項目を入力してください。<br>
        変更しない場合は、空欄のまま更新を押してください。</p>
        </div>

        <div class="form-group">
          <%= f.label :新しいパスワード（6文字以上）, class: "col-xs-6" %>
          <%= f.password_field :password, autocomplete: "new-password", class: "form-control", style: "display: inline-block; width: auto;" %>
        </div>

        <div class="form-group">
          <%= f.label :新しいパスワード（確認用）, class: "col-xs-6" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control", style: "display: inline-block; width: auto;" %>
        </div>

        <div class="actions col-xs-offset-6">
          <button class="btn btn-warning", type="submit">更新</button>
        </div>
      <% end %>

    <div class="back-link", style="margin: 15px 0 0 15px;">
      <%= link_to users_path do %>
      <i class="fas fa-chevron-circle-left"></i>&nbsp;マイページへ戻る<% end %>
    </div>
</div>
