<div class="col-xs-12">
    <h4 >パスワード再設定</h4>
    <p>会員登録時に利用したメールアドレスを入力してください。</p>

	<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  		<%= render "devise/shared/error_messages", resource: resource %>

  		<div class="form-group row">
    		<%= f.label :email, class:"col-xs-4" %>
    		<%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control",  style: "width:58%;" %>
 		</div>

  		<div class="actions row">
     		<button class="btn btn-warning col-xs-offset-4 col-xs-7", type="submit">パスワード再設定メールを送信する</button>
  		</div>
	<% end %>
<%#= render "users/shared/links" %>

</div>
